define([
    'underscore',
    'backbone',
    'namespace'
], function (_, Backbone, ns) {
    ns.define('am.purchase.model');
    am.purchase.model.PurchaseListReport = Backbone.Model.extend();

    return am;
});