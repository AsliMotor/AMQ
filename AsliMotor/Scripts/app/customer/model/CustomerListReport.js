define([
    'underscore',
    'backbone',
    'namespace'
], function (_, Backbone, ns) {
    ns.define('am.customer.model');
    am.customer.model.CustomerListReport = Backbone.Model.extend();

    return am;
});